export default function SettingsPage() {
  return (
    <div className="card p-6">
      <div className="section-title text-xs text-brand-ink">Settings</div>
      <div className="mt-4 text-sm text-ink-muted">
        Admin-configurable lists (appointment types, referral sources, payer list) will live here.
      </div>
    </div>
  );
}
